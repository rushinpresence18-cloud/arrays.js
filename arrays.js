// Create an array of colors
let colors = []
colors.push("Green", "Blue", "Red")

// Array of Numbers
let numbers = ["3", "6", "18", "20"]

// Removing the last color of the Array
let updatedcolors = colors.filter(color => color !== "Red");
console.log(updatedcolors);

// Adding a New color to the Array
let colors = ["Green", "Blue", "Red"]
colors.push ("Silver");
console.log (colors);

let colors = ["Green", "Blue", "Red"];
let numbers = ["3", "6", "18", "20"]
 
console.log (colors);
console.log (numbers);





